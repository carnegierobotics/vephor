from skbuild import setup  # This line replaces 'from setuptools import setup'
setup(
    name="pyv4",
    version="0.0.2",
    description="v4, a visualization library",
    author='Steve Landers',
    packages=['pyv4'],
    python_requires=">=3.7",
)